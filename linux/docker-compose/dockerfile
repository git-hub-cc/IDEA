# 1. 使用官方的 Maven 镜像作为基础
#    这个镜像已经包含了 Maven 和 JDK 17
FROM maven:3.9-eclipse-temurin-17

# 2. 安装 Git
#    这个镜像是基于 Debian 的，所以我们使用 apt-get 来安装。
#    RUN 命令会在构建镜像时执行。
RUN apt-get update && apt-get install -y git

# 3. 设置工作目录 (可选，但在 Dockerfile 中是好习惯)
WORKDIR /app